package model;

/** Defines the priorities supported by the task queue. */
public enum PriorityEnum {
    /** Lowest queue priority. */
    LOW,
    /** Medium queue priority. */
    MEDIUM,
    /** Highest queue priority. */
    HIGH
}
